version: '3.8'
services:
  compiler:
    build: .
    volumes:
      - .:/app
  dir-watch:
    image: debian:11-slim
    volumes:
      - .:/app
    working_dir: /app
    # По умолчанию запускаем утилиту с флагом --help
    command: ./dir-watch --help